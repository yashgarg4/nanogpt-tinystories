# --- Python virtual environment ---
.venv/
venv/

# --- Byte-compiled / caches ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# --- Tokenized datasets (large, regenerated by src/data.py) ---
*.bin
data/*.bin
# HuggingFace datasets download/cache if pointed locally
data/hf_cache/

# --- Model checkpoints (large, regenerated by training) ---
checkpoints/

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
.vscode/
.idea/

# NOTE: we intentionally DO NOT ignore samples/ — generated stories and
# loss-curve plots are committed so the training progression is visible.
